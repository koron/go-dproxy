name: Go

on: [push]

env:
  GO_VERSION: '>=1.24.0'

jobs:

  build:
    name: Build
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os:
        - ubuntu-latest
        - ubuntu-24.04-arm
        - macos-latest
        - windows-latest
    steps:

    - uses: actions/checkout@v5

    - uses: actions/setup-go@v6
      with:
        go-version: ${{ env.GO_VERSION }}

    - run: go test ./...

    - name: Build all "main" packages
      shell: bash
      run: |
        root=$(go list -f '{{.ImportPath}}' .)
        for path in $(go list -f '{{if (eq .Name "main")}}{{.ImportPath}}{{end}}' ./... ) ; do
          dir=".${path#${root}}"
          printf "building %s\t(%s)\n" $(basename "$path") $dir
          ( cd "$dir" && go build )
        done

# based on: github.com/koron-go/_skeleton/.github/workflows/go.yml
# $Hash:681012ee2e20780fbb40f995e914299c5f0f438364f8c274f66ee7c8$
